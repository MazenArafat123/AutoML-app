"""
utils/config.py

Single source of truth for constants shared across app.py and the
page modules, so MAX_SAMPLE / PALETTE aren't redefined in multiple
places.
"""

MAX_SAMPLE = 1000
PALETTE = "pastel"
